export type RegisterType = {
  first_name: string;
  last_name: string;
  email: string;
  password: string;
  vendor_type: string;
};

export type LoginType = {
  email: string;
  password: string;
};
